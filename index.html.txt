<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>For Sanjana 💖</title>

<style>
body {
  margin: 0;
  height: 100vh;
  background: linear-gradient(135deg, #ff758c, #ff7eb3);
  display: flex;
  justify-content: center;
  align-items: center;
  font-family: 'Arial', sans-serif;
  overflow: hidden;
}

.card {
  background: white;
  padding: 40px;
  border-radius: 25px;
  text-align: center;
  box-shadow: 0 25px 50px rgba(0,0,0,0.3);
}

h1 {
  color: #ff3366;
}

button {
  padding: 15px 35px;
  font-size: 18px;
  border-radius: 30px;
  border: none;
  cursor: pointer;
  transition: transform 0.2s;
}

#yes {
  background: #ff3366;
  color: white;
  margin-right: 20px;
}

#no {
  background: #ccc;
  color: #333;
  position: absolute;
}

#messageScreen {
  position: fixed;
  inset: 0;
  background: linear-gradient(135deg, #ff9a9e, #fad0c4);
  display: none;
  justify-content: center;
  align-items: center;
  text-align: center;
  z-index: 10;
}

#messageScreen h2 {
  font-size: 40px;
  color: #fff;
  text-shadow: 0 5px 15px rgba(0,0,0,0.3);
}

.heart {
  position: fixed;
  color: red;
  font-size: 24px;
  animation: floatUp 4s linear forwards;
}

@keyframes floatUp {
  0% { transform: translateY(0); opacity: 1; }
  100% { transform: translateY(-800px); opacity: 0; }
}
</style>
</head>

<body>

<div class="card">
  <h1>Sanjana 💕<br>Will you be my Valentine?</h1>
  <button id="yes">Yes 😍</button>
  <button id="no">No 🙃</button>
</div>

<div id="messageScreen">
  <h2>
    YAYYYY 💖💖💖<br><br>
    Sanjana, you just made me the happiest person alive 😘<br>
    Happy Valentine’s Day 🌹
  </h2>
</div>

<!-- Confetti -->
<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

<!-- Background Music -->
<audio autoplay loop>
  <source src="https://www.myinstants.com/media/sounds/hot-dog-hot-dog-hot-diggity-dog.mp3" type="audio/mpeg">
</audio>

<script>
const noBtn = document.getElementById("no");
const yesBtn = document.getElementById("yes");
const messageScreen = document.getElementById("messageScreen");

let scale = 1;

noBtn.addEventListener("mouseover", () => {
  scale -= 0.1;
  noBtn.style.transform = `scale(${scale})`;

  const x = Math.random() * (window.innerWidth - noBtn.offsetWidth);
  const y = Math.random() * (window.innerHeight - noBtn.offsetHeight);
  noBtn.style.left = x + "px";
  noBtn.style.top = y + "px";

  if (scale <= 0.2) {
    noBtn.style.display = "none";
  }
});

yesBtn.addEventListener("click", () => {
  messageScreen.style.display = "flex";

  confetti({
    particleCount: 250,
    spread: 90,
    origin: { y: 0.6 }
  });

  setInterval(createHeart, 200);
});

function createHeart() {
  const heart = document.createElement("div");
  heart.classList.add("heart");
  heart.innerHTML = "❤️";
  heart.style.left = Math.random() * window.innerWidth + "px";
  heart.style.bottom = "0px";
  document.body.appendChild(heart);

  setTimeout(() => heart.remove(), 4000);
}
</script>

</body>
</html>
